---
version: '6.0'
resources:
- partitions: 1
  cluster: common
  replication_factor: 1
  name: invalid_spark_foreground_topic
  type: kafka_topic
applications:
- name: punchline_fail_spark_foreground
  runtime: shiva
  command: punchlinectl
  args:
  - start
  - "--punchline"
  - invalid_punchline.yaml
  - "--runtime"
  - spark
  shiva_runner_tags:
  - common
  cluster: common
  exit:
    type: platform

