---
version: '6.0'
resources:
- partitions: 1
  cluster: common
  replication_factor: 1
  name: valid_storm_topic
  type: kafka_topic
applications:
- name: punchline_success
  runtime: shiva
  command: punchlinectl
  args:
  - start
  - "--punchline"
  - valid_punchline.yaml
  - "--runtime"
  - storm
  shiva_runner_tags:
  - common
  cluster: common
  exit:
    type: platform
    condition:
      failure:
        acks_greater_or_equal_to: 1000
      success:
        inactive_for_more_than: 5s
