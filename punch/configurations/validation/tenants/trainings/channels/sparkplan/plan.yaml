---
version: '6.0'
name: track1-spark_plan
model:
  dates:
    from_date:
      offset: "-PT1m"
      format: yyyy-MM-dd'T'HH:mmZ
    to_date:
      format: yyyy-MM-dd'T'HH:mmZ
settings:
  cron: "*/1 * * * *"

